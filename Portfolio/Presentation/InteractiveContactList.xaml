<UserControl
    x:Class="Portfolio.Presentation.InteractiveContactList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="260">

    <Grid Background="Transparent">
        <StackPanel VerticalAlignment="Center" Spacing="24" HorizontalAlignment="Center">
            
            <!-- Poetry Orb -->
            <Grid x:Name="PoetryContainer" Width="80" Height="80" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <CompositeTransform/>
                </Grid.RenderTransform>
                <Ellipse Fill="#EC4899" Opacity="0.2" Width="80" Height="80" x:Name="PoetryGlow" RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <CompositeTransform/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                <Ellipse Fill="#EC4899" Width="60" Height="60"/>
                <FontIcon Glyph="&#xE006;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Text="POETRY" FontSize="10" FontWeight="Bold" Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,-20"/>
            </Grid>

            <!-- Magnetic Buttons -->
            <Grid x:Name="GithubBtn" Width="60" Height="60" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <CompositeTransform/>
                </Grid.RenderTransform>
                <Border Background="#2D3348" CornerRadius="20" x:Name="GithubBg"/>
                <FontIcon Glyph="&#xECA7;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <Grid x:Name="LinkBtn" Width="60" Height="60" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <CompositeTransform/>
                </Grid.RenderTransform>
                <Border Background="#2D3348" CornerRadius="20" x:Name="LinkBg"/>
                <FontIcon Glyph="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <Grid x:Name="EmailBtn" Width="60" Height="60" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <CompositeTransform/>
                </Grid.RenderTransform>
                <Border Background="#2D3348" CornerRadius="20" x:Name="EmailBg"/>
                <FontIcon Glyph="&#xE715;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

        </StackPanel>
    </Grid>
</UserControl>
